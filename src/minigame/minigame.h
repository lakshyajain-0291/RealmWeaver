// minigame_runner.h
#ifndef MINIGAME_RUNNER_H
#define MINIGAME_RUNNER_H

#include <string>

// Function to run the maze game with a given rank and return the result.
int run_maze(int rank);

// Function to run the connect4 game with a given rank and return the result.
int run_connect4(int rank);

#endif // MINIGAME_RUNNER_H
